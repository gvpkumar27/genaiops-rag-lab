apiVersion: 1

providers:
  - name: "LocalDocChat Dashboards"
    orgId: 1
    folder: "LocalDocChat"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
